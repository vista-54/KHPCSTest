;(function () {
    angular
        .module('factory.surveyQuestion', [])
        .factory('surveyQuestion', surveyQuestion);

    surveyQuestion.$inject = [];

    function surveyQuestion() {
        let model = {};
    }

})();